plugins {
    id "java"
    id "com.moowork.node" version "1.3.1"
}

jar {
    from "build" into "static"
}

jar.dependsOn npm_run_build

task npmTest(type: NpmTask) {
    args = ['run', 'test:ci']
}

task test(type: Test, overwrite: true, dependsOn: npmTest)